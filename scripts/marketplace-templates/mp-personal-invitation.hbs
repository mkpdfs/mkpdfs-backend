<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: 'Georgia', serif; background: linear-gradient(135deg, #fdf4ff 0%, #fae8ff 100%); padding: 40px; min-height: 100vh; }
    .invitation { max-width: 550px; margin: 0 auto; background: white; border-radius: 20px; overflow: hidden; box-shadow: 0 25px 50px -12px rgba(0,0,0,0.15); }
    .header { background: linear-gradient(135deg, #e879f9 0%, #c026d3 100%); color: white; padding: 40px; text-align: center; }
    .event-type { font-size: 14px; text-transform: uppercase; letter-spacing: 4px; opacity: 0.9; margin-bottom: 10px; }
    .header h1 { font-size: 32px; font-weight: normal; font-style: italic; }
    .content { padding: 40px; text-align: center; }
    .host { font-size: 14px; color: #666; margin-bottom: 20px; }
    .honoree { font-size: 48px; font-family: 'Brush Script MT', cursive; color: #c026d3; margin-bottom: 20px; }
    .theme { display: inline-block; background: #fdf4ff; color: #a21caf; padding: 8px 20px; border-radius: 20px; font-size: 14px; margin-bottom: 30px; }
    .details { margin-bottom: 30px; }
    .detail-item { margin-bottom: 20px; }
    .detail-item label { display: block; font-size: 11px; text-transform: uppercase; letter-spacing: 2px; color: #999; margin-bottom: 5px; }
    .detail-item span { font-size: 18px; color: #333; }
    .venue-name { font-weight: 600; }
    .divider { width: 60px; height: 2px; background: linear-gradient(90deg, #e879f9, #c026d3); margin: 30px auto; }
    .rsvp { background: #fdf4ff; padding: 30px; margin: 0 -40px -40px; }
    .rsvp h3 { font-size: 14px; text-transform: uppercase; letter-spacing: 2px; color: #a21caf; margin-bottom: 15px; }
    .rsvp p { font-size: 14px; color: #666; margin-bottom: 5px; }
    .rsvp .contact { font-size: 16px; color: #c026d3; font-weight: 500; }
    .special { margin-top: 20px; padding: 15px; background: #fef3c7; border-radius: 8px; }
    .special p { font-size: 12px; color: #92400e; margin: 0; }
    .dress-code { margin-top: 20px; font-size: 13px; color: #666; }
    .dress-code strong { color: #333; }
  </style>
</head>
<body>
  <div class="invitation">
    <div class="header">
      <div class="event-type">{{eventType}}</div>
      <h1>{{eventTitle}}</h1>
    </div>

    <div class="content">
      <div class="host">{{hostName}} cordially invites you to celebrate</div>
      <div class="honoree">{{honoree}}</div>
      <div class="theme">{{theme}}</div>

      <div class="details">
        <div class="detail-item">
          <label>Date & Time</label>
          <span>{{date}}</span><br>
          <span>{{time}}</span>
        </div>

        <div class="detail-item">
          <label>Venue</label>
          <span class="venue-name">{{venue}}</span><br>
          <span>{{address}}</span>
        </div>
      </div>

      <div class="dress-code">
        <strong>Dress Code:</strong> {{dressCode}}
      </div>

      <div class="divider"></div>

      <div class="rsvp">
        <h3>RSVP by {{rsvpBy}}</h3>
        <p>Please respond to</p>
        <p class="contact">{{rsvpContact}}</p>
        <p class="contact">{{rsvpPhone}}</p>

        {{#if specialInstructions}}
        <div class="special">
          <p>{{specialInstructions}}</p>
        </div>
        {{/if}}
      </div>
    </div>
  </div>
</body>
</html>
