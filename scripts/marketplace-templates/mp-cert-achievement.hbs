<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: 'Georgia', serif; background: linear-gradient(135deg, #1e3a5f 0%, #0f172a 100%); padding: 40px; min-height: 100vh; }
    .certificate { background: linear-gradient(135deg, #fefce8 0%, #fef9c3 100%); max-width: 800px; margin: 0 auto; padding: 60px; position: relative; box-shadow: 0 25px 50px -12px rgba(0,0,0,0.5); }
    .corner { position: absolute; width: 100px; height: 100px; }
    .corner-tl { top: 0; left: 0; border-top: 8px solid #b45309; border-left: 8px solid #b45309; }
    .corner-tr { top: 0; right: 0; border-top: 8px solid #b45309; border-right: 8px solid #b45309; }
    .corner-bl { bottom: 0; left: 0; border-bottom: 8px solid #b45309; border-left: 8px solid #b45309; }
    .corner-br { bottom: 0; right: 0; border-bottom: 8px solid #b45309; border-right: 8px solid #b45309; }
    .header { text-align: center; margin-bottom: 30px; }
    .star { font-size: 48px; color: #b45309; margin-bottom: 10px; }
    .organization { font-size: 18px; text-transform: uppercase; letter-spacing: 6px; color: #78350f; margin-bottom: 20px; }
    .title { font-size: 48px; color: #78350f; font-weight: normal; letter-spacing: 4px; }
    .body { text-align: center; padding: 40px 0; }
    .presented { font-size: 16px; color: #666; margin-bottom: 20px; letter-spacing: 2px; }
    .recipient { font-size: 48px; color: #1e3a5f; font-family: 'Brush Script MT', cursive; margin-bottom: 30px; }
    .achievement { font-size: 24px; color: #b45309; font-weight: bold; margin-bottom: 20px; text-transform: uppercase; letter-spacing: 3px; }
    .description { font-size: 16px; color: #333; line-height: 1.8; max-width: 550px; margin: 0 auto; font-style: italic; }
    .date { text-align: center; margin: 40px 0; }
    .date span { font-size: 16px; color: #666; }
    .signatures { display: flex; justify-content: center; gap: 120px; margin-top: 50px; }
    .signature { text-align: center; }
    .signature-line { width: 180px; border-top: 2px solid #78350f; padding-top: 10px; }
    .signature-name { font-size: 18px; font-style: italic; color: #333; }
    .signature-title { font-size: 12px; color: #666; margin-top: 4px; }
    .ribbon { position: absolute; bottom: 40px; left: 50%; transform: translateX(-50%); }
    .ribbon-badge { width: 60px; height: 60px; background: linear-gradient(135deg, #b45309, #92400e); border-radius: 50%; display: flex; align-items: center; justify-content: center; box-shadow: 0 4px 6px rgba(0,0,0,0.3); }
    .ribbon-badge span { color: white; font-size: 24px; }
    .ribbon-tails { display: flex; justify-content: center; margin-top: -10px; }
    .tail { width: 20px; height: 40px; background: #b45309; }
    .tail:first-child { transform: skewX(-15deg); margin-right: 10px; }
    .tail:last-child { transform: skewX(15deg); margin-left: 10px; }
  </style>
</head>
<body>
  <div class="certificate">
    <div class="corner corner-tl"></div>
    <div class="corner corner-tr"></div>
    <div class="corner corner-bl"></div>
    <div class="corner corner-br"></div>

    <div class="header">
      <div class="star">&#9733;</div>
      <div class="organization">{{organizationName}}</div>
      <h1 class="title">{{title}}</h1>
    </div>

    <div class="body">
      <div class="presented">This award is proudly presented to</div>
      <div class="recipient">{{recipientName}}</div>
      <div class="achievement">{{achievementTitle}}</div>
      <p class="description">{{description}}</p>
    </div>

    <div class="date">
      <span>Awarded on {{awardDate}}</span>
    </div>

    <div class="signatures">
      <div class="signature">
        <div class="signature-line">
          <div class="signature-name">{{presenterName}}</div>
          <div class="signature-title">{{presenterTitle}}</div>
        </div>
      </div>
    </div>

    <div class="ribbon">
      <div class="ribbon-badge">
        <span>&#9733;</span>
      </div>
      <div class="ribbon-tails">
        <div class="tail"></div>
        <div class="tail"></div>
      </div>
    </div>
  </div>
</body>
</html>
