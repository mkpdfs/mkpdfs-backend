<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: 'Helvetica Neue', Arial, sans-serif; color: #333; }
    .brochure { max-width: 800px; margin: 0 auto; }
    .hero { background: linear-gradient(135deg, #7c3aed 0%, #4f46e5 100%); color: white; padding: 60px 40px; text-align: center; }
    .hero h1 { font-size: 48px; font-weight: 700; margin-bottom: 10px; }
    .hero .tagline { font-size: 24px; font-weight: 300; opacity: 0.9; margin-bottom: 30px; }
    .hero p { font-size: 18px; max-width: 500px; margin: 0 auto; line-height: 1.6; opacity: 0.8; }
    .features { padding: 50px 40px; background: #f9fafb; }
    .features h2 { text-align: center; font-size: 28px; color: #4f46e5; margin-bottom: 40px; }
    .features-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 30px; }
    .feature { background: white; padding: 30px; border-radius: 12px; box-shadow: 0 4px 6px rgba(0,0,0,0.05); }
    .feature-icon { width: 50px; height: 50px; background: linear-gradient(135deg, #7c3aed, #4f46e5); border-radius: 12px; display: flex; align-items: center; justify-content: center; margin-bottom: 20px; }
    .feature-icon span { color: white; font-size: 24px; }
    .feature h3 { font-size: 18px; color: #333; margin-bottom: 10px; }
    .feature p { font-size: 14px; color: #666; line-height: 1.6; }
    .pricing { padding: 50px 40px; }
    .pricing h2 { text-align: center; font-size: 28px; color: #333; margin-bottom: 40px; }
    .pricing-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; }
    .plan { border: 2px solid #e5e7eb; border-radius: 16px; padding: 30px; text-align: center; transition: all 0.3s; }
    .plan.featured { border-color: #7c3aed; transform: scale(1.05); box-shadow: 0 10px 40px rgba(124, 58, 237, 0.2); }
    .plan-name { font-size: 20px; color: #333; margin-bottom: 10px; text-transform: uppercase; letter-spacing: 2px; }
    .plan-price { font-size: 42px; color: #7c3aed; font-weight: 700; margin-bottom: 20px; }
    .plan-price span { font-size: 16px; color: #666; font-weight: normal; }
    .plan-features { list-style: none; margin-bottom: 30px; }
    .plan-features li { padding: 8px 0; font-size: 14px; color: #666; border-bottom: 1px solid #f3f4f6; }
    .plan-features li:last-child { border-bottom: none; }
    .cta-section { background: linear-gradient(135deg, #7c3aed 0%, #4f46e5 100%); color: white; padding: 50px 40px; text-align: center; }
    .cta-section h2 { font-size: 32px; margin-bottom: 20px; }
    .cta-button { display: inline-block; background: white; color: #7c3aed; padding: 16px 40px; font-size: 18px; font-weight: 600; border-radius: 8px; text-decoration: none; }
  </style>
</head>
<body>
  <div class="brochure">
    <div class="hero">
      <h1>{{productName}}</h1>
      <div class="tagline">{{tagline}}</div>
      <p>{{heroDescription}}</p>
    </div>

    <div class="features">
      <h2>Why Choose Us?</h2>
      <div class="features-grid">
        {{#each features}}
        <div class="feature">
          <div class="feature-icon">
            <span>&#10003;</span>
          </div>
          <h3>{{title}}</h3>
          <p>{{description}}</p>
        </div>
        {{/each}}
      </div>
    </div>

    <div class="pricing">
      <h2>Simple, Transparent Pricing</h2>
      <div class="pricing-grid">
        <div class="plan">
          <div class="plan-name">Starter</div>
          <div class="plan-price">${{pricing.starter.price}}<span>/mo</span></div>
          <ul class="plan-features">
            {{#each pricing.starter.features}}
            <li>{{this}}</li>
            {{/each}}
          </ul>
        </div>
        <div class="plan featured">
          <div class="plan-name">Professional</div>
          <div class="plan-price">${{pricing.professional.price}}<span>/mo</span></div>
          <ul class="plan-features">
            {{#each pricing.professional.features}}
            <li>{{this}}</li>
            {{/each}}
          </ul>
        </div>
        <div class="plan">
          <div class="plan-name">Enterprise</div>
          <div class="plan-price">${{pricing.enterprise.price}}<span>/mo</span></div>
          <ul class="plan-features">
            {{#each pricing.enterprise.features}}
            <li>{{this}}</li>
            {{/each}}
          </ul>
        </div>
      </div>
    </div>

    <div class="cta-section">
      <h2>Ready to Get Started?</h2>
      <a href="{{ctaUrl}}" class="cta-button">{{ctaText}}</a>
    </div>
  </div>
</body>
</html>
